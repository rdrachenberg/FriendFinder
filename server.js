// requrie dependancies 
var express = requrie("express"); 
var bodyParser = require("body-parser");
var path = requrie("path");
// dependancies installed 
// .gitignore file added with dependancies ignored 

